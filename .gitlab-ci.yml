image: docker:24.0.7

services:
  - docker:24.0.7-dind

variables:
  DOCKER_TLS_CERTDIR: ""
  IMAGE_TAG: "$CI_REGISTRY_IMAGE:latest"

stages:
  - build

build:
  stage: build
  script:
    - echo "Building docker image"
    - docker build -t "$IMAGE_TAG" .
